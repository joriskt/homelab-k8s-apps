apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bookstack-pvc
spec:
  storageClassName: ceph-csi-cephfs
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
